 <h1>All Questions</h1>
 <div "col-md-4">
 <%= link_to "New Questions", new_question_path(@question), class: 'btn btn-success' %>
 </div>
  <% @questions.each do |q| %>
     <div class="media">
      <div class="media-body">
        <h4 class="media-heading">
          <%= link_to q.title,q %>
        </h4>
      </div>
    </div>
<% end %>

